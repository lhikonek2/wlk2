{
  "name": "WhisperLiveKit CPU Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "onCreateCommand": "sudo apt-get update && sudo apt-get install -y ffmpeg",

  "postCreateCommand": "cd /workspaces/wlk2/WhisperLiveKit && pip install -e . && pip install torch --index-url https://download.pytorch.org/whl/cpu && pip install faster-whisper websockets python-multipart",

  "forwardPorts": [8000],

  "portsAttributes": {
    "8000": {
      "label": "WLK Server",
      "onAutoForward": "notify"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.pylance",
        "charliermarsh.ruff",
        "anthropic.claude-code",
        "eamodio.gitlens",
        "oderwat.indent-rainbow",
        "wayou.vscode-todo-highlight"
      ]
    }
  }
}